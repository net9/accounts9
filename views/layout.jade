!!!
html
  head
    title= pageTitle(title)
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel='shortcut icon', href='/favicon.ico')
    script(type="text/javascript", src="/javascripts/jquery.min.js")
  body
    #container
      #header
        #menu
          - if (session.userinfo)
            != msg('logged-in-as', session.userinfo.username)
            a#logout(href="/logout")= msg('Logout')
        a#logo(href="/")
          img(src="/images/net9.gif", width="130", height="130")
          img(src="/images/accounts.svg", width="300", height="100")
      #content
        h1= title
        != partial('partials/flash', flashArray)
        #body!= body
        .clear
      #footer!= msg('footer-text')
    script(type="text/javascript", src="/javascripts/script.js")

